ingress:
  enabled: true
  ingressClassName: nginx
  hostname: keycloak.127-0-0-1.nip.io
  pathType: Prefix

postgresql:
  enabled: true

  # TODO : Secret Data => Ansible Vault
  auth:
    username: keycloak
    password: password

# TODO : Secret Data  => Ansible Vault
auth:
  adminUser: admin
  adminPassword: password
