apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: esdata
  namespace: transactions
spec:
  storageClassName: local-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
